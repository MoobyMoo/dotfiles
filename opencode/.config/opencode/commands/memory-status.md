---
description: Show what durable profile memory a new session will use
agent: personal-admin
---

Read `~/.config/opencode/memory/current-profile.md` and summarize:
- what a fresh session should already know
- what is still unresolved
- what should be updated if the user's preferences have changed

Extra request from user:
$ARGUMENTS
